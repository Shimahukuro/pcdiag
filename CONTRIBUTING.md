# コントリビューションガイド

`pcdiag`への関心と協力に感謝します。本プロジェクトは初期開発段階にあり、データ仕様や診断規則が変更される場合があります。

## セキュリティと機密情報

脆弱性は公開Issueへ投稿せず、[`SECURITY.md`](SECURITY.md)に従って非公開で報告してください。

`pcdiag`の収集成果物には、デバイスインスタンスID、モデル名、ドライバー情報など、機器や環境の識別につながる情報が含まれる可能性があります。次のファイルやディレクトリを、未確認のままIssueやPull Requestへ添付しないでください。

- 診断セッションのディレクトリ一式
- `collection.json`、`status.json`、`diagnosis.json`
- `report.html`および各種ログ
- 実機情報が表示された画面画像

再現資料が必要な場合は、個人情報、シリアル番号、デバイス識別子、パス、ネットワーク情報などを削除し、必要最小限の内容だけを共有してください。

## Issue

- 既存Issueに同じ内容がないか確認してください。
- バグ報告にはIssueフォームを使用してください。
- 実際の動作、期待する動作、再現手順を簡潔に記載してください。
- 大きな仕様変更や新機能は、実装前にIssueで方針を相談してください。

## Pull Request

- 1つのPull Requestには、関連する変更だけを含めてください。
- 変更理由と確認方法を説明してください。
- データ仕様や診断規則を変更する場合は、関連ドキュメントとテストも更新してください。
- 既存の警告やテスト失敗を無関係な変更で隠さないでください。
- 機密情報や実機の未加工データを、ソース、テストデータ、コミット履歴へ含めないでください。

## 開発環境と確認

Rust stableを使用します。Pull Requestを送る前に、可能な範囲で次を実行してください。

```console
cargo fmt --all -- --check
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
```

Windows固有部分を変更した場合は、可能であればWindows 10またはWindows 11で次も確認してください。

```powershell
cargo build --release --locked -p pcdiag
.\target\release\pcdiag.exe --help
```

管理者権限や特定のハードウェアが必要で確認できない項目は、Pull Requestへその旨を記載してください。

## 言語方針

- Issue、Pull Request、設計資料は日本語を基本とします。
- Rustの型名、関数名、変数名などの識別子は英語を使用します。
- コードコメントは周囲の記述に合わせ、日本語で簡潔に記述します。
- ユーザー向けメッセージとドキュメントでは、意味が明確な自然な日本語を使用します。
- コミットメッセージは、変更内容を簡潔に表す英語または日本語を推奨します。

## 行動規範

参加者は[`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)に従ってください。
